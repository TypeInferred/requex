/**
 * A common storage key for addressing the source of a reduction chain in auxillary state.
 * @type {String}
 */
export const SOURCE = '__SOURCE';

/**
 * A common storage key for addressing an inner-source of a reduction chain in auxillary state.
 * @type {String}
 */
export const INNER_SOURCE = '__INNER_SOURCE';

/**
 * A common storage key for addressing the value of a reducer in auxillary state.
 * @type {String}
 */
export const VALUE = '__VALUE';

/**
 * A common storage key for addressing the root value in the auxillary state.
 * @type {String}
 */
export const ROOT = 'ROOT';

/**
 * A common storage key prefix for addressing the reducer arguments in the auxillary state.
 * @type {String}
 */
export const ARGS_PREFIX = '__ARGS_';

/**
 * A common storage key prefix for addressing one of the sources of a reduction chain in auxillary state.
 * @type {String}
 */
export const SOURCE_PREFIX = '__SOURCE_';

/**
 * A common storage key prefix for addressing extra state in the auxillary state object.
 */
export const EXTRA = '__EXTRA';